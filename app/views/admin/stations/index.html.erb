<h1>All Stations</h1>
<h2>Admin View</h2>

<%= link_to "Add New Station", new_admin_station_path %>

<% @stations.each do |station|  %>
  <ul>
    <h3> <%= link_to "#{station.name}", admin_station_path(station) %></h3>
      <li>Dock Count: <%= station.dock_count %></li>
      <li>City: <%= station.city %></li>
      <li>Installation Date: <%= station.installation_date %></li>
      <li><%= link_to "Edit", edit_admin_station_path(station) %></li>
      <li><%= link_to "Delete", admin_station_path(station), method: "delete" %></li>
  </ul>
  <% end %>
